# CODEX WORK DIRECTIVE — BATCH 4-EXTENDED · SWS-REM-DIR-20260828
## Builder seat: codex · authorized by OPERATOR-INSTRUCTIONS.log ENTRY 004 · zero placeholders

**Operator:** paste everything between the markers into a Codex session with its working directory at `D:\producttion software 2\` and full file access. ENTRY 004 is already recorded; nothing to fill.

---BEGIN PASTE---

You are the BUILDER seat, `codex (local harness)`, executing Batch 4-Extended under directive SWS-REM-DIR-20260828 R2 and Annex A. You previously audited this workspace as the ENTRY-003 auditor seat; your discrepancy register is now your work order. **Role shift is explicit:** you build; you do not audit or grade your own changes — the validator and reviewer seats do. You never write reviewer PASS. Everything you produce is CANDIDATE. You do not stop between items and you ask the operator nothing; park-and-continue per Annex A E-1..E-6.

## §0 AUTHORIZATION AND BOOT — verify before any write

1. Read `D:\producttion software 2\release-planning\OPERATOR-INSTRUCTIONS.log`: confirm ENTRIES 001–004 present, and that ENTRY 004 grants BATCH-4-EXTENDED with the item set P-D2 P-D3 P-D4 P-D5 P-D6 N-11 N-14 N-15 N-13, OD-22=ANNOTATE, builder=codex. Log SHA-256 at recording: `a00d1ed0d61e9bf638991fae85330881ef37800cbbc37dcd244ea54ba0934ac8`. Absent or different item set → produce nothing, report.
2. Hash-verify custody: `SWS-REM-DIR-20260828-CANDIDATE-R2.md` = `9061c2e8a40bcff4258f77692ff0e23ffebde98eaa638446a22a7aa914b3691d`; `LOOP-PROTOCOL-20260828.md` = `acae8fb8b9b4d673f28ae5b6d4b74fbfed0c86e0e70bfa1f86638237219bae44`.
3. Worktree `D:\producttion software 2\release-worktree`: HEAD `5a52946`, branch main, clean porcelain (your audit restored it clean; if node_modules-era ignored files show as untracked that is expected and handled in P-D6).
4. Read: `ADJUDICATION-RECORD-R3-20260828.md` (your findings, adjudicated — governs P-D2..P-D6), `PUNCH-LIST-V3-20260828.md` (items B-2/B-3/B-5/B-6 → N-11/N-13/N-14/N-15), `UI-VISUAL-BASELINE-SPEC-20260828.md` + `design\UI-TARGET-20260828.png` (governs N-14), and your own `audit-codex\CODEX-AUDIT-REPORT.md` evidence paths.

## §1 BOUNDARIES — absolute, unchanged from R2 §2 plus batch specifics

WRITE only to: `release-worktree\`, `release-planning\bundles\`. READ everything. NEVER touch: the frozen ZIPs and their sidecars, existing custody documents, `audit-codex\` (your former audit output is now frozen evidence), `D:\Product Software\`. **Never write or restore source files via PowerShell redirection or Set-Content** (UTF-16 hazard, cause of AUD-1) — use Python `open(path,'w',encoding='utf-8')`/`'wb'` or `cmd /c` redirection; after any file restore verify the first bytes are not FF FE. Run Python tests with `PYTHONDONTWRITEBYTECODE=1`/`-B`. No network calls except where an item explicitly permits none are needed (no item in this batch needs network). No billed anything, no GPU compute, no service launches, no model loads beyond what N-11's verification explicitly states.

## §2 PER-ITEM DISCIPLINE — every item, no exceptions

Fail-before evidence (reproduce the defect exactly, captured to the bundle) → minimal diff (no unrelated cleanup, no drive-by edits) → pass-after evidence → affected local suite run with measured counts → changed-file list → one commit per item, message `B4E <item-id>: <summary>` → bundle folder `release-planning\bundles\BATCH4\<item-id>\` with NOTES.md. An item whose pass criteria you cannot check mechanically parks with a dossier; the loop continues.

## §3 THE QUEUE — strict order; P-D3 is LAST, nothing follows it

**1 · P-D2 (HIGH) — managed-process import regression.** `modules/sow/adapters/frontier/process_tree.py:15` `from adapters.cmd_shim import assert_cmd_shim_argv_safe` breaks the direct-execution job-member path (`ModuleNotFoundError: No module named 'adapters'` — your own `evidence/08-managed-process-clean-command.json`). Fail-before: re-run your clean-command probe, capture the traceback. Fix WITHOUT weakening the guard: robust import with fallback, e.g. try the package import, on ImportError load `cmd_shim` by path relative to `__file__` (`Path(__file__).resolve().parents[1] / "cmd_shim.py"` via importlib), binding the same `assert_cmd_shim_argv_safe`. The hostile-argv refusal must behave byte-identically. Pass-after: (a) clean `run_managed_process` succeeds end-to-end — returncode 0, real stdout, zero surviving pids; (b) M-4 timing probe passes with genuine success (~sub-second, non-empty output); (c) all 17 `test_cmd_shim_metacharacters` tests pass on host Python and 3.12; (d) one end-to-end hostile refusal through the managed path. Add the missing integration test (clean managed success) to `modules/sow/tests/integration/` so this hole cannot reopen. Commit.

**2 · N-15 — shell pre-flight npm detection.** The shell reports `npm [WinError 2]` with npm installed (it is a `.cmd` shim, not an .exe; your B3-2 run used it successfully). Locate the probe (`shell/src/probe.py` / `run_preflight`). Fail-before: reproduce the false-negative (invoke the probe on this host, capture the WinError result). Fix: resolve `.cmd`/`.bat` shims explicitly — `shutil.which` covers PATHEXT; if the probe execs the tool for a version string, exec the resolved shim path via `cmd /c` with a fixed argv validated by the L-1 `assert_cmd_shim_argv_safe` guard (import it properly; you just made that robust in item 1). No `shell=True`. Pass-after: probe reports npm with its version on this host; regression test with a fake `.cmd` shim on a temp PATH proving detection and guarded resolution; shell tests that cover preflight still green. Commit.

**3 · N-11 — Debate seat VRAM fit.** Measured live: default seat `phi4:14b` = 12 GB model on an 8 GB card → Ollama 51/49 CPU/GPU split, ~5 GB resident at idle keep-alive, CPU-bound inference. Locate the seat/model defaults in the candidate Debate tree (`modules/debate/` config — search for `phi4`). Fail-before: cite the config bytes naming phi4:14b. Change the default seat model to `qwen3:8b` (installed on this host, ~5 GB fully GPU-resident). Touch ONLY the default-model configuration — no engine changes. If Debate pins models in more than one coupled place (config + tests + manifest), change all coupled sites so its config-integrity test stays green; run the Debate test file(s) that validate config (`test_v1_2_1_config_integrity.py` and neighbors) with measured results. Do NOT load any model to verify — static config + tests only (model-load verification is a T-2 live leg). Mark the bundle: "candidate default; final ruling folds into OD-9; release notes must state: default seats sized for full residency on 8 GB cards." Commit.

**4 · P-D4 — shell BUILD-MANIFEST refresh.** `shell/BUILD-MANIFEST.txt` pins `src/server.py` at `6eeb30e3…` (the corrupted pre-R-1 blob); actual is `54d391c5…`. Determine how the manifest is generated (the shell suite regenerated it in run 01 — find that mechanism in `shell/tests/`); regenerate through the sanctioned mechanism, else update the line to the measured hash. Then verify EVERY line of the manifest against actual bytes and correct any other stale entries found (list them in the bundle). Fail-before = the stale line + your audit citation; pass-after = full-manifest verification script output, all lines matching. Commit.

**5 · P-D5 — v1.2 P1 doc corrections, OD-22=ANNOTATE.** (a) `BUILD-DIRECTIVE-SWS-UI-001.md:79`: correct the live table row — Debate Table v1.2.1-hardening, release ZIP `Debate_Table_v1.2.1_Hardening_20260823_143520.zip`, sha256 `d03ba417914e1465898f5144cc5735afb92f7f6da5e846e0a968fe48c531d265`, sidecar VERIFIED. (b) `docs/ADR-003.md` and `docs/DISCOVERY.md`: add ONE dated line at the top of each — `> Historical record of the v1.2-P1 era (annotated 2026-08-28 per OD-22): Debate is v1.2.1-hardening as of RELEASE-MANIFEST.json; original text preserved below.` — and change nothing else in them. Pass-after: `rg "v1\.2\s+P1"` output showing only the two annotated historical files and quarantined evidence lanes. Commit.

**6 · P-D6 — worktree hygiene + gate policy.** Delete every ignored cache artifact runs created — all `__pycache__`/`*.pyc`, `.pytest_cache`, test-created sqlite/db — EXCEPT `node_modules` (required by the desktop suite; gitignored; excluded from archives by construction). Run the package-boundary gate twice, capture both: clean `git archive` of HEAD (must be exit 0, 0 violations) and live tree. Write the policy declaration into the bundle and into `tools/release/README-GATE-POLICY.md` (new, small): "the gate's release verdict binds to the clean archive of the release commit; live-tree scans are operational hygiene." Commit (the README + any .gitignore adjustment only; deletions are of untracked files).

**7 · N-13 — adapter path-rebase mechanism.** All seven `shell/modules/*.json` adapters hard-code `D:/Product Software/Production Workspace/...` roots (Distillery/Token Center also `C:/Users/Sslaw/...python.exe`) — proven this morning to make the shell launch old-tree module code. Build the config-driven rebase: (a) introduce a single workspace-root source of truth — `shell/config/install.json` (new) with `{"modules_root": "<abs path>", "python_312": "<abs path>"}`; (b) `tools/release/rebase_adapters.py` (new): reads install.json, rewrites the adapter JSONs' path fields from a declared template mapping (explicit field list per adapter — no blind string replace), writes `.previous.json`-style backups of the originals per the supersession convention, idempotent, `--dry-run` mode, refuses paths that don't exist; (c) run it against THIS worktree (`modules_root` = `D:\producttion software 2\release-worktree\Production Workspace`-equivalent — use the actual worktree layout: `D:\producttion software 2\release-worktree`), so the candidate shell launches candidate modules; original old-tree adapter files preserved as backups. (d) Tests: dry-run diff correctness, idempotency, nonexistent-path refusal, and a post-rebase assertion that no adapter JSON contains `D:/Product Software` or `C:/Users/Sslaw`. NOTE in the bundle: modules need their venvs provisioned before they can actually START from the worktree (environment work, T-2/E-3 — out of this batch's scope); the rebase makes the paths correct, not the environments present. Commit.

**8 · N-14 — UI visual baseline.** Governed entirely by `UI-VISUAL-BASELINE-SPEC-20260828.md` and the target capture. Step 1 (mandatory before writing any code): determine current state — render the worktree shell's markup/CSS statically (read `shell/` static assets + templates; do not launch services) and diff structurally against the spec. If the tree already implements the target (THEME-BASELINE-v3 lineage), the item closes as a parity verification with evidence, no code. If a delta exists, implement ONLY the delta under the spec's hard constraints: local/self assets only (background image as a local static file or data URI — record its hash), CSP and security headers byte-identical, presentation-only diffs (HTML/CSS/static JS rendering paths), contrast preserved (h16 class), keyboard nav untouched. Pass-after: shell test suite green (measured), a static served-asset inventory proving zero external references, before/after DOM/CSS diff in the bundle. Visual screenshot parity is a T-2 attended check — mark it QUEUED-T2 in the bundle, not done. Commit.

**9 · P-D3 (LAST — after every other byte change is committed).** Regenerate the three module provenance records (debate, sow, distillery) with `tools/release/generate_module_provenance.py` against the FINAL tree; supersede prior records per convention (previous preserved); regenerate `RELEASE-MANIFEST.json` with every embedded digest measured from final bytes — including the sow `package-lock.json` hash (currently stale at `573eba33…`, actual `6a79bbcc…` pre-batch; measure fresh) and the new/changed files from this batch. Then run ALL gates and capture measured output: package boundary (clean archive), provenance cross-hash, release-manifest check, governance BOM, model consistency (note: N-11 changed Debate's model — if the consistency gate covers Debate seats, update its registry inputs as part of this item, minimally), innerHTML audit, and the full release-tool unittest aggregate — **the aggregate must be green; if anything fails, fix within batch scope and regenerate again.** Append the standing rule to the batch report verbatim: "Any batch that changes module bytes ends with provenance/manifest regeneration and a full gate re-run; a manifest generated before the last byte change is stale by definition." Commit.

## §4 OUTPUT

Per item: `bundles\BATCH4\<item>\` (fail-before, diff stats, pass-after, measured suite output, NOTES.md). Then `bundles\BATCH4\BATCH-REPORT.md`: item table (id, commit, status CANDIDATE/PARKED, measured counts), the final commit chain tail from `5a52946`, and the T-2 queue additions (N-14 visual parity check; N-13 venv provisioning prerequisite; N-11 live model-load verification). Append a BATCH-4E section to `bundles\LOOP-RUN-REPORT.md` — do not rewrite existing sections. Final worktree state: clean porcelain, all commits pushed to the local branch, no reviewer PASS anywhere. End every report with:
`BUILDER CLAIM: no gate submitted for reviewer evaluation; no PASS asserted.`

Begin with §0 now. Run the queue in order to exhaustion, then stop.

---END PASTE---
