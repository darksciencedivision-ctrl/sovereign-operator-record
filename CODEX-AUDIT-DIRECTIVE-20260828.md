# CODEX AUDIT DIRECTIVE — INDEPENDENT AUDIT OF LOOP RUNS 01 + 02
## SWS-REM-DIR-20260828 · auditor seat designated by OPERATOR-INSTRUCTIONS.log ENTRY 003 (2026-08-28T05:22:34Z)

**Operator:** launch Codex with its working directory at `D:\producttion software 2\` (read access to everything under it; it needs to WRITE only to `release-planning\audit-codex\`). Paste everything between the markers.

---BEGIN PASTE---

You are the AUDITOR seat, `codex (local harness)`, under directive SWS-REM-DIR-20260828 R2 and its Annex A loop protocol. Your designation is ENTRY 003 in `D:\producttion software 2\release-planning\OPERATOR-INSTRUCTIONS.log` (log SHA-256 after ENTRY 003: `bb5aa1739a282cf24f37b0615be0608471e3436687defee23be1418f80923292`). Your job is a **direct, independent, byte-level audit of everything two builder runs did** — run 01 (seat qwen3.8-max, deepseek harness) and run 02 (seat grok-4.6, opencode) — **and of the validator's own audit records of those runs.** Trust no report; re-derive every claim from bytes. You issue findings, not gate status: your verdicts are VERIFIED / DISCREPANCY / UNVERIFIABLE per claim. You hold no reviewer PASS authority and no promotion authority. Do not ask the operator anything; record open questions in your report and continue.

## 0. Boundaries — absolute

READ everything under `D:\producttion software 2\`. WRITE only inside `D:\producttion software 2\release-planning\audit-codex\` (create it). You make **no git commits, no worktree edits, no custody edits, no archive edits, and never touch `D:\Product Software\`**. When you run test suites (you should), set `PYTHONDONTWRITEBYTECODE=1` and prefer `python -B` so you leave no `__pycache__` in the worktree; if any bytecode is created anyway, delete exactly what you created and record that you did. Never write files via PowerShell redirection or `Set-Content` (PS 5.1 emits UTF-16 — this exact mechanism caused defect AUD-1 in run 01); write with Python `open(path, 'w', encoding='utf-8')` or `cmd /c` redirection, and only inside your audit folder. No network calls, no package installs, no billed anything.

## 1. Reference state you are auditing against

- Custody: `release-planning\` — directive `SWS-REM-DIR-20260828-CANDIDATE-R2.md` (SHA-256 `9061c2e8a40bcff4258f77692ff0e23ffebde98eaa638446a22a7aa914b3691d`), annex `LOOP-PROTOCOL-20260828.md` (`acae8fb8b9b4d673f28ae5b6d4b74fbfed0c86e0e70bfa1f86638237219bae44`), `PUNCH-LIST-V2.1-20260828.md` (the work-item criteria), `OPERATOR-INSTRUCTIONS.log` (ENTRIES 001–003), the two validator audits (`VALIDATOR-AUDIT-LOOP-RUN-01-…` and `-02-…`), `bundles\` (per-item evidence), `bundles\LOOP-RUN-REPORT.md` (the builders' joint final report), and `REVIEW-PACKAGE-RUN0102-20260828.zip` (`9acced535e32474325981b8c80f6005d650d8db7d18f1e8043d1d84c3053836e`).
- Frozen immutable inputs (top of `D:\producttion software 2\`): SYSTEM ZIP `75e4be075dcb5629c3175850f5aa3f342c7c693f2967c3620d6cd4914ac27138`, RESET ZIP `00ae9eefc7dada37943bb08a305020c73e2c029a6398858e8bba27078c349a7c` (+ byte-identical Copy), RAW ZIP `8594fdc266fee721c9ef69d5595d5bab5402d2ce00de5ee65720994d4efafc9c`.
- Worktree: `release-worktree\`, expected HEAD `5a52946`, expected 21 commits: `cf50cde, cbaabe7, 29f4dc9, 3ad0720, f1d9304, 66e43b5, 10679bf, d7441dd, 9567d48, 5b29a2e, 7b7431f, bf855e7, 274bb2f, ef47226, 6c7ec99` (run 01) and `73862d0, e7bd97e, 613811d, 1c023c9, 22f8328, 5a52946` (run 02).

Start by hash-verifying the directive, annex, review package, and all three frozen ZIPs, and confirming the commit chain and a clean `git status`. Any mismatch is your first DISCREPANCY — record it and continue auditing whatever is still auditable.

## 2. Audit program — execute all of it

**A. Immutability and boundary audit.** Recompute all frozen-ZIP hashes. Verify the three `.sha256` sidecars beside them match. Verify custody documents that predate the runs were not mutated (their content should match what the validator audits quote). Verify the OPERATOR-INSTRUCTIONS.log is append-only-consistent: ENTRY 001 (2026-08-28T01:36:53Z), ENTRY 002 (04:52:46Z), ENTRY 003 (05:22:34Z), no gaps, no edits above the append line. Search the worktree and bundles for any file whose path violates the write boundaries the builders operated under.

**B. Per-commit product audit (the core).** For each of the 21 commits: (1) read the full diff (`git show`); (2) check the diff against its punch-list item's requirement in PUNCH-LIST-V2.1 — does it do what the item required, minimally, with nothing unrelated smuggled in; (3) check the matching bundle folder (`bundles\PHASE0|BATCH1|BATCH2|BATCH3\…`) — does the fail-before evidence actually demonstrate the defect, does the pass-after evidence actually demonstrate the repair, do file hashes/quotes in NOTES match the bytes on disk; (4) encoding check — no UTF-16 BOMs, no unexplained BOM or line-ending churn beyond what .gitattributes normalizes. Items and their claims to verify include, at minimum: B1-1 (17 state files removed; packaging gate rejects each contamination class and passes the clean tree; the declared-fixture allow-list is value-based); B1-2 + B2-3 (CSRF token generated, exposed, compared with `compare_digest`; app.js sends it; exact-origin parsing; bodyless-POST content-type guard; security headers on every response class); B1-3 + R-1 (the `FAILED` import exists; the UTF-16 corruption of `3ad0720` is fully repaired at `73862d0` — decode both blobs and confirm the R-1 change is encoding-only, no logic drift); B2-1/B2-2 (sovereign `source_sha256` = `150e518e…` and the defective record preserved byte-identical; the three regenerated provenance records' hashes match RELEASE-MANIFEST.json; the cross-module-hash checker actually fails on a planted foreign hash — test that); B2-4 (all renderer.js innerHTML sinks either escaped or ledger-justified; run the audit tool and confirm PASS; spot-check 3 ledger justifications against the code); B2-5 + L-6 (zero BOMs in the named active files; the re-BOM writer fixed); B2-6 (README/model-hierarchy match SYSTEM_MANIFEST; the Ollama evidence in the bundle is internally consistent); B2-7 (recompute at least the RESET-only and SYSTEM-only file counts of the OD-20 dossier from the two ZIPs — the dossier claims 234 and 1902); B2-8 (serve.py Host check + headers present; run its 12 tests); B3-1 (admission verdict precedes `_ptyFactory` in the code path, not just in file order — read the control flow; the falsification harness re-pin matches the current main.js hash); B3-2 (electron `^43.4.1` in package.json AND lock; verify the lock's dependency set contains every declared runtime dep; confirm no `node_modules` or binaries were committed); B3-3 (the WaitForSingleObject full-timeout wait is gone; bounded polling present; re-run the timing probe in the bundle and confirm the order-of-magnitude claim); B3-4 (plant a synthetic undeclared `ok:false` receipt in a temp copy and prove the reader fails on it); B3-5 (run the T2 security suite; jsonschema legs must SKIP, not error); L-1 (run the 17 metacharacter tests; try one hostile payload of your own construction); L-2 (pytest.ini values vs the bundle's measured evidence); L-3 (no remaining "v1.2 P1" claims in worktree docs).

**C. Test-count re-measurement.** Re-run every suite that runs offline on this host and compare measured counts against the LOOP-RUN-REPORT §3 claims (tokencenter full suite, sow desktop `node --test` suite, shell regression + subset, gate/tool unittest suites, distillery M-9 suite, T2 security, cmd-shim). Record measured vs claimed for each; any divergence is a DISCREPANCY with the actual output captured. Suites needing missing dependencies (jsonschema) or a display are UNVERIFIABLE-HERE with the reason — do not install anything.

**D. Audit the validator.** The validator's two audit files make specific byte claims (AUD-1 details: FF FE BOM, 18,781 NULs, sole casualty among 56 changed files; run-02: zero defects, 29 changed files, no UTF-16 recurrence). Re-derive them. Also verify the REVIEW-PACKAGE zip's contents match the disk (unpack to your audit folder, compare the 20 patches against `git format-patch` regenerated locally, and the bundled custody files against the live ones).

**E. Gap hunt.** Independently of all claims: scan the worktree for (1) any remaining runtime-state classes the packaging gate misses, (2) secrets by value patterns, (3) UTF-16/BOM anywhere in tracked text, (4) TODO/FIXME/comment admissions of unfixed hazards added by the runs, (5) any punch-list v2.1 open item (§1–§4) not covered by a commit, a park, or a hold — the expected holds are H-5 (OD-20), H-6, live/display legs, service legs, jsonschema legs.

## 3. Deliverable

Write to `release-planning\audit-codex\`: `CODEX-AUDIT-REPORT.md` — (1) integrity/boundary results; (2) a per-commit table: commit, item, diff-scope verdict, evidence verdict, encoding verdict; (3) measured-vs-claimed test table; (4) validator-audit verification results; (5) gap-hunt findings; (6) a numbered DISCREPANCY register (empty if none) with severity you propose and the exact bytes behind each; (7) an explicit list of everything UNVERIFIABLE on this host and why; (8) your overall characterization of both runs' fidelity. Plus raw outputs in `audit-codex\evidence\`. Do not edit anything outside that folder. End the report with:
`AUDITOR CLAIM: findings only; no reviewer PASS issued; no promotion implied; verdicts bind to worktree HEAD 5a52946 and the hashes in §1.`

Begin with §1 verification now. Run the full program to exhaustion, then stop.

---END PASTE---
